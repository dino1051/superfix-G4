1
2
3
4
5
6 - BUG: la funcion importada en profesores.controller se llama "crearProfesor", no "crearProfesores"
7 - BUG: en crearProfesor del models falto agregar el telefono en el INSERT
8 - BUG: en el endpoin delete si el profesor no existe aun asi aparece que fue eliminado
9
10
11-config/db.ts: línea 10 => password: process.env.DB_PASSWORD,
12-models/estudiantes.model.ts: linea 16=> 'WHERE id'
13-controllers/estudiantes.controller.ts: linea 59=> const id = Number(req.params.id);
14-models/estudiantes.model.ts: linea 40=>  const edad = data.edad || actual.edad; (antes actualizaba con 0 cuando no se registraba explicitamente)
15-models/estudiantes.model.ts: linea 12=>   return result.rows; (no ordenaba asc)
16
17
18
19
20
21 Validar que ID se numero.
22 Validad que al sumar calificaciones al inicio empiece con 0
23 solo se puede calcular el promedio si hay elementos.
24 odren entre estudiante y profesor estaban intercambiados
16 cursos.controller linea 28, if (curso) -> if (!curso) condicion incorrecta
17 cursos.controller.ts linea 2, no habia importacion de Request, Response y Next
18 cursos.model.ts linea 45, query("DELETE FROM cursos", [id]) -> ("DELETE FROM cursos WHERE id = $1", [id])
19 cursos.model.ts linea 39, [descripcion,nombre , id] - > [nombre, descripcion, id] nombre es el primer elemento en aparecer
20 cursos.model.ts linea 10, curso -> cursos, la tabla se llama cursos
21
22
23
24
25
26-controllers/estudiante.controller.ts: falto: import type { Request, Response, NextFunction } from "express";
27-controllers/estudiantes.controller.ts:  falto la condicion: if (estudiantes.length === 0) {res.status(404).json({ error: "Estudiante no encontrado" });return;}
28-models/estudiante.modelts: En buscarEstudiantesPorNombre =>  "... WHERE nombre ILIKE $1 ORDER BY id"
29-config/swagger.ts: Falto agregar en estudiantes: "/estudiantes/buscar": 

